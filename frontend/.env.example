# API (use full URL if not using Vite proxy)
VITE_API_URL=http://localhost:4000/api

# stripe | razorpay — must match backend DEFAULT_PAYMENT_PROVIDER for checkout flows
VITE_PAYMENT_PROVIDER=razorpay

# Stripe publishable key (pk_test_...) when using Stripe
VITE_STRIPE_PUBLISHABLE_KEY=
